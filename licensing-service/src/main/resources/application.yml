spring:
  application:
    name: licensing-service
  profiles:
    active: dev
  cloud:
    config:
      uri: http://localhost:8071
      enabled: true
  config:
    import: "configserver:"

management:
  endpoints:
    web:
      exposure:
        include: health, info, refresh, mappings
    enabled-by-default: true